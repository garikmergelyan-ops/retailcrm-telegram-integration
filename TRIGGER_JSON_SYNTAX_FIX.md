# 🔧 Исправление: Ошибка парсинга JSON от триггера

## ❌ Проблема

RetailCRM отправляет невалидный JSON:
```
SyntaxError: Unexpected token 'o', ..." "order": order 1076"... is not valid JSON
```

Это значит, что переменная `{{order}}` в триггере не подставилась правильно, и RetailCRM отправил текст вместо JSON объекта.

## ✅ Решения

### Вариант 1: Исправить синтаксис в триггере (РЕКОМЕНДУЕТСЯ)

Проблема в том, что RetailCRM не понимает синтаксис `{{order}}`. Попробуйте разные варианты:

#### Вариант 1A: Использовать отдельные поля
В поле "custom query body" вставьте:
```
{
  "orderId": "{{order.id}}",
  "orderNumber": "{{order.number}}",
  "status": "{{order.status}}",
  "statusCode": "{{order.statusCode}}"
}
```

#### Вариант 1B: Попробовать другой синтаксис
В зависимости от версии RetailCRM, попробуйте:
- `{order}` (без двойных фигурных скобок)
- `$order`
- `[order]`
- `order` (без скобок вообще)

#### Вариант 1C: Использовать query параметры
1. В настройках триггера выберите **"in query string"**
2. Добавьте параметры:
   - `order_id`: `{{order.id}}`
   - `order_number`: `{{order.number}}`
   - `status`: `{{order.status}}`

### Вариант 2: Использовать API (уже реализовано в коде)

Код уже обновлен для автоматического получения данных через API, если JSON невалидный:
- Код пытается найти ID заказа в запросе
- Если находит - делает API запрос для получения полных данных
- Для этого нужны API ключи в переменных окружения на Render.com

**Проверьте, что в Render.com Dashboard → Environment есть:**
- `RETAILCRM_API_KEY_1` (для Account 1)
- `RETAILCRM_API_KEY_3` (для Account 3)

## 🔍 Как проверить

1. **Обновите триггер** с правильным синтаксисом
2. **Создайте тестовый заказ** и аппрувьте его
3. **Проверьте логи** - должны увидеть либо валидный JSON, либо успешное получение данных через API

## 📋 Что должно быть в логах после исправления

### Если JSON валидный:
```
✅ Найден заказ в req.body.order
📋 Данные заказа:
   ID: 12345
   Number: ORD-001
   Status: approved
```

### Если JSON невалидный, но API работает:
```
⚠️ Найден возможный ID заказа в невалидном JSON: 12345
   Пытаемся получить данные через API...
✅ Данные заказа получены через API по ID: 12345
```

## ⚠️ Важно

Код уже обновлен для обработки таких ошибок:
- Сервер не падает при невалидном JSON
- Автоматически пытается получить данные через API
- Логирует все для отладки

Но лучше настроить триггер правильно, чтобы данные приходили сразу в валидном JSON формате.

---

**Следующий шаг:** Попробуйте разные варианты синтаксиса в триггере RetailCRM или убедитесь, что API ключи настроены на Render.com.

