# ✅ РАБОЧЕЕ РЕШЕНИЕ: Правильная настройка триггера RetailCRM

## 🔍 АНАЛИЗ ПРОБЛЕМЫ

**Проблема:** Триггеры не срабатывают вообще - нет POST запросов в логах.

**Причина:** Неправильный синтаксис переменных в триггере или триггер не активен.

## ✅ ПРАВИЛЬНОЕ РЕШЕНИЕ (из официальной документации RetailCRM)

Согласно документации RetailCRM, для отправки полного JSON объекта заказа нужно использовать:

```
{{ order|json_encode }}
```

**НЕ** `{{order}}` - это не работает!

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Проверьте, что триггер активен

1. Зайдите в RetailCRM: `https://aff-gh.retailcrm.ru`
2. Перейдите в **Настройки** → **Триггеры**
3. Найдите ваш триггер
4. **Убедитесь, что триггер ВКЛЮЧЕН** (должна быть галочка или переключатель)

### Шаг 2: Проверьте журнал действий

1. В RetailCRM перейдите в **Настройки** → **Журнал действий**
2. Найдите записи о срабатывании триггера
3. Если есть ошибки - исправьте их

### Шаг 3: Настройте триггер правильно

#### Вариант A: Custom Query Body (РЕКОМЕНДУЕТСЯ)

1. Откройте триггер для редактирования
2. В разделе **"Send parameters"** выберите **"custom query body"**
3. **ВАЖНО:** Вставьте ТОЧНО такой текст (БЕЗ слова "json" и БЕЗ обратных кавычек):

```json
{
  "order": {{ order|json_encode }},
  "event": "orderStatusChange"
}
```

**Ключевое отличие:** Используйте `{{ order|json_encode }}` вместо `{{order}}`!

4. Проверьте HTTP заголовки:
   - **Header name**: `Content-Type`
   - **Header value**: `application/json`

5. Сохраните триггер

#### Вариант B: Если `{{ order|json_encode }}` не работает

Попробуйте альтернативные варианты синтаксиса:

1. `{{ order }}` (с пробелами)
2. `{{order|json}}`
3. `{{ order.toJson() }}`
4. `$order` (без фигурных скобок)

#### Вариант C: Query параметры (если body не работает)

1. Выберите **"in query string"**
2. Добавьте параметры:
   - `order_id` = `{{order.id}}`
   - `order_number` = `{{order.number}}`
   - `status` = `{{order.status}}`
   - `account_url` = `https://aff-gh.retailcrm.ru`

3. Код автоматически получит полные данные через API

### Шаг 4: Проверьте URL триггера

1. Убедитесь, что URL правильный:
   ```
   https://retailcrm-telegram-integration.onrender.com/webhook/retailcrm
   ```

2. Проверьте, что URL доступен:
   - Откройте в браузере: `https://retailcrm-telegram-integration.onrender.com/test`
   - Должен вернуться JSON с сообщением "Server is running!"

### Шаг 5: Протестируйте

1. **Создайте тестовый заказ** в RetailCRM
2. **Аппрувьте его** (измените статус на "approved")
3. **Проверьте журнал действий** в RetailCRM:
   - Должна быть запись о срабатывании триггера
   - Если есть ошибка - посмотрите детали
4. **Проверьте логи на Render.com:**
   - Должен быть POST запрос на `/webhook/retailcrm`
   - Если запроса нет - триггер не срабатывает

## 🔍 ДИАГНОСТИКА

### Если триггер не срабатывает вообще:

1. **Проверьте условия триггера:**
   - Событие: "Изменение статуса заказа"
   - Условие: "Статус заказа = approved" (или код статуса)
   - Убедитесь, что статус заказа действительно "approved"

2. **Проверьте журнал действий:**
   - Есть ли записи о срабатывании триггера?
   - Есть ли ошибки?

3. **Проверьте права API ключа:**
   - API ключ должен иметь права на выполнение триггеров

4. **Попробуйте создать новый триггер:**
   - Иногда старые триггеры могут не работать после обновлений

### Если триггер срабатывает, но данные не приходят:

1. **Проверьте логи на Render.com:**
   - Должен быть POST запрос
   - Проверьте, что в `req.body` есть данные

2. **Проверьте синтаксис в триггере:**
   - Используйте `{{ order|json_encode }}`
   - Проверьте, что нет лишних кавычек

3. **Проверьте HTTP заголовки:**
   - Должен быть `Content-Type: application/json`

## 📞 ЕСЛИ НЕ РАБОТАЕТ

1. **Обратитесь в поддержку RetailCRM:**
   - Спросите: "Как правильно отправить полный объект заказа в теле HTTP-запроса триггера?"
   - Укажите версию RetailCRM

2. **Проверьте версию RetailCRM:**
   - В некоторых версиях синтаксис может отличаться
   - Обновите RetailCRM до последней версии

3. **Используйте альтернативный подход:**
   - Отправляйте только ID и номер заказа
   - Код автоматически получит полные данные через API

---

## ✅ ЧЕКЛИСТ

- [ ] Триггер активен (включен)
- [ ] Условие триггера правильное (статус = approved)
- [ ] URL правильный и доступен
- [ ] HTTP заголовок `Content-Type: application/json` добавлен
- [ ] В custom query body используется `{{ order|json_encode }}`
- [ ] Триггер сохранен
- [ ] Протестирован на реальном заказе
- [ ] Проверен журнал действий в RetailCRM
- [ ] Проверены логи на Render.com

---

**ВАЖНО:** Используйте `{{ order|json_encode }}` - это правильный синтаксис из документации RetailCRM!

