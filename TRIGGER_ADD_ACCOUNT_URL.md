# ✅ Правильная настройка триггера: добавление account_url

## 🎯 Проблема

Сейчас код пытается определить аккаунт автоматически, но это неэффективно. Лучше настроить триггер так, чтобы он сам отправлял информацию о том, от какого аккаунта пришел запрос.

## ✅ Решение: добавить account_url в параметры триггера

### Для Account 1 (Ghana):

1. **Зайдите в RetailCRM** → **Настройки** → **Триггеры**
2. **Откройте триггер для Account 1**
3. **Найдите раздел "Send parameters"** → **"in query string"**
4. **Добавьте еще один параметр:**
   - **Parameter name**: `account_url`
   - **Parameter value**: `https://aff-gh.retailcrm.ru`
   - (Или используйте переменную, если поддерживается: `{{account.url}}`)

### Для Account 3 (SlimTeaPro):

1. **Откройте триггер для Account 3**
2. **В разделе "Send parameters"** → **"in query string"**
3. **Добавьте параметр:**
   - **Parameter name**: `account_url`
   - **Parameter value**: `https://slimteapro-store.retailcrm.ru`

## 📋 Итоговый список параметров для каждого триггера

### Account 1 (Ghana):
- `order_id` = `{{order.id}}`
- `order_number` = `{{order.number}}`
- `status` = `{{order.status}}`
- `account_url` = `https://aff-gh.retailcrm.ru` ⬅️ **ДОБАВЬТЕ ЭТОТ**

### Account 3 (SlimTeaPro):
- `order_id` = `{{order.id}}`
- `order_number` = `{{order.number}}`
- `status` = `{{order.status}}`
- `account_url` = `https://slimteapro-store.retailcrm.ru` ⬅️ **ДОБАВЬТЕ ЭТОТ**

## ✅ Преимущества

1. **Точное определение аккаунта** - код сразу знает, какой API ключ использовать
2. **Быстрее** - не нужно пробовать оба аккаунта
3. **Надежнее** - нет ошибок "Parameter 'site' is missing"
4. **Проще** - один API запрос вместо двух попыток

## 🔄 После настройки

1. Сохраните триггеры
2. Создайте тестовый заказ и аппрувьте его
3. Проверьте логи - должны увидеть:
   ```
   ✅ Order found in req.query
   🔑 Using API key for: https://aff-gh.retailcrm.ru
   ✅ Full order data received via API
   ```

## ⚠️ Если не можете добавить account_url

Если в триггере нет возможности добавить account_url, код все равно будет работать:
- Сначала попробует Account 1
- Если не найдет заказ - попробует Account 3
- Но это медленнее и может давать ошибки

---

**Рекомендация:** Добавьте `account_url` в параметры триггера - это самое правильное решение!

