# 🔍 Где НА САМОМ ДЕЛЕ находятся Webhooks в RetailCRM

## ❌ Важная информация

**Collector - это НЕ webhooks!** Это совершенно другая функция.

## 📋 Что такое Collector?

**Collector** - это JavaScript-счетчик для вашего сайта, который:
- ✅ Показывает, какие клиенты онлайн в интерфейсе RetailCRM
- ✅ Собирает контактные данные посетителей при уходе с сайта
- ✅ Интегрирует формы обратной связи
- ❌ **НЕ отправляет уведомления о заказах** - это не то, что вам нужно!

**Документация Collector:** https://help.retailcrm.pro/Developers/Collector

## ✅ Где РЕАЛЬНО находятся Webhooks?

**Webhooks в RetailCRM настраиваются через раздел "Триггеры"!**

### Правильный путь:
1. **RetailCRM** → **Настройки** (или **Администрирование**) → **Триггеры**
2. Нажать **"+Триггер"** или **"Создать триггер"**
3. Настроить триггер для отправки HTTP-запроса на ваш сервер

## 🎯 Пошаговая инструкция настройки Webhook через Триггеры

### Шаг 1: Найти раздел "Триггеры"
- Зайдите в RetailCRM
- Перейдите в **Настройки** (или **Администрирование**)
- Найдите раздел **"Триггеры"** (может быть в подменю "Автоматизация" или "Интеграции")

### Шаг 2: Создать новый триггер
1. Нажмите **"+Триггер"** или **"Создать триггер"**
2. Укажите название: **"Уведомление о аппруве заказа"**

### Шаг 3: Настроить событие
1. В разделе **"События"** или **"Условия"** выберите:
   - **"Изменение статуса заказа"** или
   - **"Обновление заказа"** или
   - **"Создание заказа"**

2. Добавьте условие (если доступно):
   - **Статус заказа = "approved"** (или код статуса аппрува)

### Шаг 4: Настроить действие (HTTP-запрос)
1. В разделе **"Действия"** выберите **"HTTP-запрос"** или **"Webhook"**
2. Укажите параметры:
   - **URL**: `https://your-app.onrender.com/webhook/retailcrm`
   - **Метод**: `POST`

3. **Настройте HTTP-headers:**
   - Нажмите **"Add header"**
   - **Header name**: `Content-Type`
   - **Header value**: `application/json`
   - Нажмите **"Add"** или **"Сохранить"**

4. **Настройте Send parameters:**
   - Выберите **"custom query body"** (для отправки JSON)
   - В поле для тела запроса можно указать JSON-шаблон или оставить пустым (RetailCRM отправит данные заказа автоматически)
   
   **Альтернатива:** Если "custom query body" не работает, попробуйте **"in query body (urlencode)"** - ваш сервер должен обработать оба формата

### Шаг 5: Сохранить триггер
- Нажмите **"Сохранить"** или **"Создать"**
- Триггер начнет работать сразу

## 🔄 Повторить для каждого аккаунта

Создайте отдельный триггер для каждого аккаунта:
- **Account 1 (Ghana)**: `aff-gh.retailcrm.ru`
- **Account 3 (SlimTeaPro)**: `slimteapro-store.retailcrm.ru`

## ⚠️ Если раздела "Триггеры" нет

Если вы не видите раздел "Триггеры", возможные причины:

1. **Недостаточно прав доступа**
   - Обратитесь к администратору аккаунта
   - Попросите права на настройку триггеров/автоматизации

2. **Ограничения тарифного плана**
   - Некоторые функции доступны только в определенных тарифах
   - Свяжитесь с поддержкой RetailCRM для уточнения

3. **Другое название раздела**
   - Может называться "Автоматизация"
   - Может быть в разделе "Интеграции" → "Автоматизация"
   - Может быть в разделе "Настройки" → "Автоматизация"

## 📞 Что делать, если не нашли "Триггеры"

### Вариант 1: Обратиться в поддержку RetailCRM
Напишите им:
```
Добрый день!

Хочу настроить webhook для получения уведомлений об изменении статуса заказов.
В разделе "Администрирование" → "Интеграция" я вижу только "API access keys" и "Collector".
Где находится раздел для настройки webhooks/триггеров?
Нужно ли активировать эту функцию отдельно?
```

### Вариант 2: Проверить через API
Возможно, webhooks можно настроить через API. Проверьте документацию:
- https://docs.retailcrm.ru/Developers/API/APIVersions
- Поищите методы для работы с триггерами/webhooks

### Вариант 3: Использовать альтернативный подход
Если webhooks недоступны, можно:
- Продолжить использовать API polling (но с оптимизацией)
- Использовать более частый polling (каждую минуту вместо 5-10 минут)
- Оптимизировать запросы (меньше данных, фильтры)

## 🔍 Как проверить, что триггер работает

1. **Создайте тестовый заказ** в RetailCRM
2. **Аппрувьте его** (измените статус на "approved")
3. **Проверьте логи вашего сервера** на Render.com
4. **Проверьте, пришел ли POST запрос** на `/webhook/retailcrm`

Если запрос пришел - триггер работает! ✅

## 📊 Формат данных от триггера

RetailCRM может отправлять данные в разных форматах. Проверьте в логах, что именно приходит:

```javascript
// В вашем server.js добавьте логирование:
app.post('/webhook/retailcrm', (req, res) => {
    console.log('=== WEBHOOK RECEIVED ===');
    console.log('Headers:', req.headers);
    console.log('Body:', JSON.stringify(req.body, null, 2));
    console.log('========================');
    
    // Ваша обработка...
    
    res.status(200).json({ success: true });
});
```

## ✅ Резюме

1. **Collector ≠ Webhooks** - это разные функции
2. **Webhooks настраиваются через "Триггеры"**, а не через "Вебхуки"
3. **Путь**: Настройки → Триггеры → Создать триггер → HTTP-запрос
4. **Если не нашли** - обратитесь в поддержку RetailCRM

---

**Следующий шаг:** Найдите раздел "Триггеры" в вашем аккаунте RetailCRM и создайте триггер для отправки HTTP-запроса при изменении статуса заказа на "approved".

