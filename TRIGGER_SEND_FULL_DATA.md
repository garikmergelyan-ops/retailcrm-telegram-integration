# 🎯 ПРАВИЛЬНАЯ НАСТРОЙКА ТРИГГЕРА: Отправка полных данных заказа

## ❌ Текущая проблема

Триггер отправляет только `order_id`, `order_number`, `status` в query параметрах. API не может найти заказ по ID (404), и поиск по номеру тоже не работает.

## ✅ ПРАВИЛЬНОЕ РЕШЕНИЕ: Отправка полных данных в теле запроса

RetailCRM **МОЖЕТ** отправлять полные данные заказа в теле запроса, если правильно настроить триггер.

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Откройте триггер для редактирования

1. Зайдите в RetailCRM: `https://aff-gh.retailcrm.ru` (или другой аккаунт)
2. Перейдите в **Настройки** → **Триггеры**
3. Откройте ваш триггер для редактирования

### Шаг 2: Удалите старые query параметры

1. Найдите раздел **"Send parameters"** или **"Параметры запроса"**
2. Если выбрано **"in query string"** - **УДАЛИТЕ ВСЕ ПАРАМЕТРЫ**:
   - Удалите `order_id`
   - Удалите `order_number`
   - Удалите `status`
   - Удалите `account_url` (если есть)

### Шаг 3: Настройте отправку данных в теле запроса

1. В разделе **"Send parameters"** выберите **"custom query body"** (пользовательское тело запроса)

2. **ВАЖНО:** В поле для тела запроса вставьте **ТОЧНО** такой JSON (БЕЗ слова "json" и БЕЗ обратных кавычек):

```json
{
  "order": {{order}},
  "event": "orderStatusChange"
}
```

**ИЛИ** если RetailCRM не понимает `{{order}}`, попробуйте:

```json
{
  "orderId": "{{order.id}}",
  "orderNumber": "{{order.number}}",
  "orderStatus": "{{order.status}}",
  "order": {{order}}
}
```

**ИЛИ** самый простой вариант (если ничего не работает):

```json
{{order}}
```

### Шаг 4: Проверьте HTTP заголовки

1. Убедитесь, что есть заголовок:
   - **Header name**: `Content-Type`
   - **Header value**: `application/json`

2. Если заголовка нет - добавьте его:
   - Нажмите **"Add header"**
   - **Header name**: `Content-Type`
   - **Header value**: `application/json`
   - Сохраните

### Шаг 5: Сохраните триггер

1. Нажмите **"Сохранить"** или **"Создать"**
2. Убедитесь, что триггер **активен**

## 🔍 АЛЬТЕРНАТИВНЫЙ ВАРИАНТ: Если "custom query body" не работает

Если RetailCRM не поддерживает `{{order}}` в custom query body, попробуйте:

### Вариант A: Использовать "in query body (urlencode)"

1. Выберите **"in query body (urlencode)"**
2. Добавьте параметры:
   - **Parameter name**: `order`
   - **Parameter value**: `{{order}}` (или `{{order.id}}`, `{{order.number}}` и т.д.)

### Вариант B: Отправить отдельные поля в body

1. Выберите **"in query body (urlencode)"**
2. Добавьте параметры:
   - `order_id`: `{{order.id}}`
   - `order_number`: `{{order.number}}`
   - `order_status`: `{{order.status}}`
   - `order_customer`: `{{order.customer}}`
   - `order_items`: `{{order.items}}`
   - `order_delivery`: `{{order.delivery}}`

## 🧪 ПРОВЕРКА

После настройки:

1. **Создайте тестовый заказ** в RetailCRM
2. **Аппрувьте его** (измените статус на "approved")
3. **Проверьте логи на Render.com:**
   - Должно быть: `"body": { "order": { ... } }` (с полными данными заказа)
   - НЕ должно быть: `"body": {}` (пустое тело)

## ⚠️ ВАЖНО

- Если в логах видите `"body": {}` - значит триггер не настроен правильно
- Если видите `"body": { "order": "order 12345" }` - значит `{{order}}` не подставился, попробуйте другой синтаксис
- Если видите `"body": { "order": { "id": 12345, ... } }` - **ЭТО ПРАВИЛЬНО!** ✅

## 📞 Если не работает

1. Попробуйте разные варианты синтаксиса:
   - `{{order}}`
   - `{order}`
   - `$order`
   - `[order]`

2. Проверьте документацию RetailCRM для вашей версии

3. Обратитесь в поддержку RetailCRM с вопросом: "Как отправить полный объект заказа в теле HTTP-запроса триггера?"

---

**Цель:** Получить полные данные заказа в `req.body.order`, чтобы не делать API запросы!

