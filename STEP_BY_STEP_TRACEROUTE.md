# Пошаговая инструкция: Выполнение трассировки маршрута

## Вариант 1: Через скрипт (САМЫЙ ПРОСТОЙ) ⭐ Рекомендуется

### Шаг 1: Загрузить скрипт на Render.com

1. **Откройте файл `traceroute-simple.js`** в вашем проекте
2. **Скопируйте весь код** из файла
3. **Закоммитьте и запушьте** в репозиторий:

```bash
# В терминале (локально)
cd "/Users/garikmergelan/Integration RetailCRM"
git add traceroute-simple.js
git commit -m "Add traceroute script for network diagnostics"
git push origin main
```

4. **Дождитесь деплоя** на Render.com (обычно 1-2 минуты)

### Шаг 2: Запустить скрипт на Render.com

**Вариант A: Через Render.com Shell (если доступен)**

1. Зайдите в **Render.com Dashboard**
2. Откройте ваш сервис (RetailCRM Integration)
3. Найдите раздел **"Shell"** или **"Console"**
4. Выполните команду:
   ```bash
   node traceroute-simple.js
   ```
5. Дождитесь завершения (может занять 2-3 минуты)
6. Результаты будут выведены в консоль и сохранены в файл `traceroute-results.txt`

**Вариант B: Через SSH (если настроен)**

1. Подключитесь к серверу через SSH:
   ```bash
   ssh <your-render-ssh-url>
   ```
2. Перейдите в директорию проекта:
   ```bash
   cd /opt/render/project/src
   ```
3. Запустите скрипт:
   ```bash
   node traceroute-simple.js
   ```
4. Дождитесь завершения
5. Результаты будут в файле `traceroute-results.txt`

**Вариант C: Через временный эндпоинт (если нет доступа к Shell)**

1. Добавьте временный эндпоинт в `smart-polling-server.js` (см. Вариант 3 ниже)
2. Откройте в браузере:
   - `https://your-app.onrender.com/debug/traceroute?host=aff-gh.retailcrm.ru`
   - `https://your-app.onrender.com/debug/traceroute?host=slimteapro-store.retailcrm.ru`
3. Скопируйте результаты из браузера

### Шаг 3: Получить результаты

**Если использовали скрипт:**
- Результаты будут в файле `traceroute-results.txt` на сервере
- Скопируйте содержимое файла или скачайте его (если есть доступ к файлам)

**Если использовали эндпоинт:**
- Скопируйте текст из браузера

### Шаг 4: Отправить результаты поддержке

1. Откройте файл `traceroute-results.txt` или скопируйте результаты
2. Создайте ответ поддержке RetailCRM (см. шаблон ниже)
3. Вставьте результаты трассировки в ответ
4. Отправьте

---

## Вариант 2: Через команду напрямую (если есть SSH доступ)

### Шаг 1: Подключиться к серверу

```bash
ssh <your-render-ssh-url>
```

### Шаг 2: Выполнить команды трассировки

```bash
# Трассировка до первого аккаунта
traceroute -n -m 30 -w 5 aff-gh.retailcrm.ru

# Подождать 3 секунды

# Трассировка до второго аккаунта
traceroute -n -m 30 -w 5 slimteapro-store.retailcrm.ru
```

### Шаг 3: Сохранить результаты

Скопируйте вывод команд в текстовый файл или напрямую в ответ поддержке.

---

## Вариант 3: Через временный эндпоинт в сервере

### Шаг 1: Добавить эндпоинт в smart-polling-server.js

Откройте файл `smart-polling-server.js` и добавьте перед `app.listen()`:

```javascript
// ВРЕМЕННЫЙ ЭНДПОИНТ ДЛЯ ТРАССИРОВКИ (удалить после использования)
app.get('/debug/traceroute', async (req, res) => {
    const { exec } = require('child_process');
    const util = require('util');
    const execPromise = util.promisify(exec);
    
    const hostname = req.query.host || 'aff-gh.retailcrm.ru';
    
    try {
        console.log(`Выполняю трассировку до ${hostname}...`);
        
        const { stdout, stderr } = await execPromise(`traceroute -n -m 30 -w 5 ${hostname}`, {
            timeout: 180000
        });
        
        if (stderr) {
            console.error('Предупреждения:', stderr);
        }
        
        res.type('text/plain');
        res.send(`Трассировка маршрута до ${hostname}\n` +
                 `Выполнено: ${new Date().toISOString()}\n` +
                 `Сервер: Render.com (Linux)\n` +
                 `Команда: traceroute -n -m 30 -w 5 ${hostname}\n\n` +
                 stdout);
    } catch (error) {
        res.status(500).type('text/plain').send(`Ошибка: ${error.message}`);
    }
});
```

### Шаг 2: Закоммитить и задеплоить

```bash
git add smart-polling-server.js
git commit -m "Add temporary traceroute endpoint for diagnostics"
git push origin main
```

### Шаг 3: Дождаться деплоя

Обычно 1-2 минуты.

### Шаг 4: Открыть в браузере

1. Откройте первый аккаунт:
   ```
   https://your-app.onrender.com/debug/traceroute?host=aff-gh.retailcrm.ru
   ```
2. Скопируйте результаты
3. Откройте второй аккаунт:
   ```
   https://your-app.onrender.com/debug/traceroute?host=slimteapro-store.retailcrm.ru
   ```
4. Скопируйте результаты

### Шаг 5: Удалить эндпоинт

После получения результатов:
1. Удалите добавленный код из `smart-polling-server.js`
2. Закоммитьте и запушьте:
   ```bash
   git add smart-polling-server.js
   git commit -m "Remove temporary traceroute endpoint"
   git push origin main
   ```

---

## Шаблон ответа поддержке RetailCRM

```
Добрый день!

Выполнил трассировку маршрута с нашего сервера (Render.com, Linux).
Использовал команду traceroute (Linux эквивалент tracert в Windows).

Результаты трассировки:

=== Account 1 (Ghana) - aff-gh.retailcrm.ru ===

[ВСТАВИТЬ РЕЗУЛЬТАТЫ ТРАССИРОВКИ ДЛЯ ПЕРВОГО АККАУНТА]

=== Account 3 (SlimTeaPro) - slimteapro-store.retailcrm.ru ===

[ВСТАВИТЬ РЕЗУЛЬТАТЫ ТРАССИРОВКИ ДЛЯ ВТОРОГО АККАУНТА]

Прошу проверить проблемные узлы (если есть) и помочь решить проблему с timeout ошибками.

С уважением,
[Ваше имя]
```

---

## Рекомендуемый порядок действий (ПРОСТОЙ ПУТЬ)

1. ✅ **Закоммитьте скрипт** `traceroute-simple.js` в репозиторий
2. ✅ **Запушьте** изменения на GitHub
3. ✅ **Дождитесь деплоя** на Render.com
4. ✅ **Откройте Render.com Shell** (если доступен) или используйте Вариант 3
5. ✅ **Запустите** `node traceroute-simple.js`
6. ✅ **Скопируйте результаты** из файла `traceroute-results.txt` или из консоли
7. ✅ **Отправьте результаты** поддержке RetailCRM по шаблону выше

---

## Что делать, если traceroute не работает?

Если команда `traceroute` не найдена на Render.com:

1. **Попробуйте ping:**
   ```bash
   ping -c 10 aff-gh.retailcrm.ru
   ping -c 10 slimteapro-store.retailcrm.ru
   ```

2. **Используйте онлайн-сервисы:**
   - https://www.yougetsignal.com/tools/visual-tracert/
   - https://www.ultratools.com/tools/traceRoute
   
   Но учтите: это покажет путь от их серверов, а не от вашего Render.com сервера.

3. **Попросите поддержку Render.com** помочь с установкой traceroute (если возможно)

---

## Важные замечания

- ⏱️ **Трассировка может занять 2-3 минуты** - это нормально
- 📊 **Результаты покажут путь пакетов** от вашего сервера до RetailCRM
- 🔍 **Ищите узлы с большими задержками** (>100ms) или потерями пакетов (*)
- 🗑️ **Удалите временный код** после использования (если использовали эндпоинт)

---

## Вопросы?

Если что-то не работает:
1. Проверьте логи на Render.com
2. Убедитесь, что скрипт задеплоен
3. Попробуйте другой вариант из списка выше


