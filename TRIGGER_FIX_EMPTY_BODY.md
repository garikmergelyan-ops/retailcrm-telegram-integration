# 🔧 Исправление: Триггер отправляет пустой запрос

## ❌ Проблема

Триггер в RetailCRM отправляет POST запрос, но тело запроса пустое:
```
"content-length": "0"
"body": {}
```

Это значит, что триггер не настроен на отправку данных заказа.

## ✅ Решение

### Вариант 1: Настроить триггер правильно (РЕКОМЕНДУЕТСЯ)

1. **Зайдите в RetailCRM** → **Настройки** → **Триггеры**
2. **Откройте ваш триггер** для редактирования
3. **Найдите раздел "Send parameters"** или **"Параметры запроса"**
4. **Выберите "custom query body"** (пользовательское тело запроса)
5. **В поле для тела запроса вставьте JSON-шаблон (БЕЗ слова "json" и без обратных кавычек):**

```
{
  "order": {{order}},
  "event": "orderStatusChange"
}
```

**Важно:** Вставляйте только содержимое между фигурными скобками, без слова "json" и без символов \`\`\`

Или более простой вариант (если RetailCRM поддерживает переменные) - вставляйте БЕЗ слова "json":

```
{
  "orderId": "{{order.id}}",
  "orderNumber": "{{order.number}}",
  "status": "{{order.status}}",
  "statusCode": "{{order.statusCode}}"
}
```

6. **Сохраните триггер**

### Вариант 2: Использовать query параметры

Если RetailCRM не поддерживает отправку данных в теле запроса:

1. **В настройках триггера** выберите **"in query string"**
2. **Добавьте параметры:**
   - `order_id`: `{{order.id}}`
   - `order_number`: `{{order.number}}`
   - `status`: `{{order.status}}`

3. **Сохраните триггер**

**Примечание:** Код уже обновлен для поддержки query параметров и автоматического получения данных через API.

### Вариант 3: Проверить документацию RetailCRM

RetailCRM может использовать другой синтаксис для переменных в триггерах:
- `{{order}}` 
- `{order}`
- `$order`
- `[order]`

Проверьте документацию RetailCRM или попробуйте разные варианты.

## 🔍 Как проверить

После настройки:

1. **Создайте тестовый заказ** в RetailCRM
2. **Аппрувьте его**
3. **Проверьте логи на Render.com** - должны увидеть данные заказа в `req.body` или `req.query`

## 📋 Что должно быть в логах после исправления

Вместо:
```
"body": {}
```

Должно быть:
```
"body": {
  "order": {
    "id": 12345,
    "number": "ORD-001",
    "status": "approved",
    ...
  }
}
```

## ⚠️ Важно

Код уже обновлен для работы с пустыми запросами:
- Если данные не приходят в теле запроса, код попытается получить их через API
- Но для этого нужны API ключи в переменных окружения на Render.com
- Лучше настроить триггер правильно, чтобы данные приходили сразу

---

**Следующий шаг:** Обновите настройки триггера в RetailCRM, чтобы он отправлял данные заказа в теле запроса.

