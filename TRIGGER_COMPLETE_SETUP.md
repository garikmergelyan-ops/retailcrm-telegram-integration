# 📋 Полная пошаговая инструкция настройки триггера в RetailCRM

## 🎯 Цель

Настроить триггер в RetailCRM так, чтобы при аппруве заказа отправлялся HTTP-запрос с данными заказа на ваш сервер.

## ✅ Пошаговая инструкция

### Шаг 1: Вход в RetailCRM

1. Зайдите в ваш аккаунт RetailCRM
2. Перейдите в **Настройки** (или **Администрирование**)
3. Найдите раздел **"Триггеры"** (может быть в подменю "Автоматизация")

### Шаг 2: Создание нового триггера

1. Нажмите **"+Триггер"** или **"Создать триггер"**
2. Укажите название: **"Уведомление о аппруве заказа"**

### Шаг 3: Настройка события

1. В разделе **"События"** или **"Условия"** выберите:
   - **"Изменение статуса заказа"** или
   - **"Обновление заказа"**

2. Добавьте условие (если доступно):
   - **Статус заказа = "approved"** (или код статуса аппрува)
   - Или: **Статус заказа изменился на "approved"**

### Шаг 4: Настройка действия (HTTP-запрос)

1. В разделе **"Действия"** выберите **"HTTP-запрос"** или **"Webhook"**

2. **Основные параметры:**
   - **URL**: `https://retailcrm-telegram-integration.onrender.com/webhook/retailcrm`
   - **Метод**: `POST`

3. **HTTP-headers:**
   - Нажмите **"Add header"**
   - **Header name**: `Content-Type`
   - **Header value**: `application/json`
   - Сохраните заголовок

4. **Send parameters (Параметры запроса):**

   **ВАРИАНТ A: Использовать query параметры (РЕКОМЕНДУЕТСЯ)**

   - Выберите **"in query string"** (в строке запроса)
   - Нажмите **"Add parameter"**
   - Добавьте параметры:
     - **Parameter name**: `order_id`
     - **Parameter value**: `{{order.id}}`
   - Нажмите **"Add parameter"** еще раз:
     - **Parameter name**: `order_number`
     - **Parameter value**: `{{order.number}}`
   - Нажмите **"Add parameter"** еще раз:
     - **Parameter name**: `status`
     - **Parameter value**: `{{order.status}}`

   **ВАРИАНТ B: Использовать body (urlencode)**

   - Выберите **"in query body (urlencode)"**
   - Нажмите **"Add parameter"**
   - Добавьте те же параметры, что и в варианте A

   **ВАРИАНТ C: Использовать custom query body (если поддерживается)**

   - Выберите **"custom query body"**
   - Вставьте (БЕЗ слова "json" и без обратных кавычек):
     ```
     {
       "orderId": "{{order.id}}",
       "orderNumber": "{{order.number}}",
       "status": "{{order.status}}"
     }
     ```

   **⚠️ ВАЖНО:** Если RetailCRM не поддерживает переменные `{{order.id}}`, попробуйте:
   - `{order.id}` (без двойных фигурных скобок)
   - `$order.id`
   - `[order.id]`
   - Или просто `order.id`

### Шаг 5: Сохранение триггера

1. Нажмите **"Сохранить"** или **"Создать"**
2. Убедитесь, что триггер **активен** (должна быть галочка или переключатель)

## 🔍 Проверка настройки

### Что должно быть в триггере:

✅ **Событие:** Изменение статуса заказа → Статус = "approved"  
✅ **Действие:** HTTP-запрос  
✅ **URL:** `https://retailcrm-telegram-integration.onrender.com/webhook/retailcrm`  
✅ **Метод:** POST  
✅ **HTTP-headers:** `Content-Type: application/json`  
✅ **Параметры:** `order_id`, `order_number`, `status` (или в body)  

## 🧪 Тестирование

1. **Создайте тестовый заказ** в RetailCRM
2. **Аппрувьте его** (измените статус на "approved")
3. **Проверьте логи на Render.com:**
   - Dashboard → Ваш сервис → Logs
   - Должны увидеть входящий запрос с данными

4. **Проверьте Telegram** - должно прийти уведомление

## ⚠️ Если не работает

### Проблема: Данные не приходят

**Решение:**
1. Проверьте, что триггер активен
2. Проверьте URL - должен быть правильный
3. Проверьте логи - что именно приходит
4. Попробуйте другой вариант отправки параметров (query string вместо body)

### Проблема: Невалидный JSON

**Решение:**
1. Используйте вариант A (query string) - он надежнее
2. Или убедитесь, что переменные подставляются правильно
3. Код автоматически попытается получить данные через API, если JSON невалидный

### Проблема: Заказ не обрабатывается

**Решение:**
1. Проверьте, что статус заказа действительно "approved"
2. Проверьте логи - должен быть виден статус заказа
3. Убедитесь, что API ключи настроены на Render.com (для получения данных через API)

## 📋 Чек-лист настройки

- [ ] Триггер создан
- [ ] Событие: Изменение статуса заказа
- [ ] Условие: Статус = "approved"
- [ ] Действие: HTTP-запрос
- [ ] URL правильный
- [ ] Метод: POST
- [ ] HTTP-header: Content-Type: application/json
- [ ] Параметры настроены (order_id, order_number, status)
- [ ] Триггер активен
- [ ] Протестирован на реальном заказе

## 🔗 Дополнительная информация

- **Ваш webhook URL:** `https://retailcrm-telegram-integration.onrender.com/webhook/retailcrm`
- **Тестовый endpoint:** `https://retailcrm-telegram-integration.onrender.com/test`
- **Логи:** Render.com Dashboard → Ваш сервис → Logs

---

**После настройки:** Создайте тестовый заказ, аппрувьте его и проверьте логи!

